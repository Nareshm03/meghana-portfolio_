export interface HeroContent {
  headlineLines: [string, string]; // manual two-line break is a locked copy decision — never auto-wrapped
  subheadline: string;
  proofCaption: string;
  ctaLabel: string;
  ctaHref: string;
  proofImageAlt: string;
}
